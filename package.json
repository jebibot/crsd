{
  "name": "crsd",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "prettier": "^2.8.8"
  },
  "scripts": {
    "lint": "prettier --check .",
    "lint:fix": "prettier --write ."
  }
}
