{
  "name": "crsd",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "prettier": "^3.0.2"
  },
  "scripts": {
    "lint": "prettier --check .",
    "lint:fix": "prettier --write ."
  }
}
